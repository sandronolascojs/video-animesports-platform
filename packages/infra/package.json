{
	"name": "@video-platform-challenge/infra",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "alchemy dev",
		"deploy": "STAGE=prod alchemy deploy",
		"deploy:dev": "alchemy deploy ./alchemy.run.ts --stage dev --env-file .env",
		"deploy:dev:ci": "alchemy deploy ./alchemy.run.ts --stage dev",
		"destroy": "alchemy destroy",
		"check-types": "tsc --noEmit"
	},
	"dependencies": {
		"@video-platform-challenge/types": "workspace:*",
		"dotenv": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@video-platform-challenge/config": "workspace:*",
		"alchemy": "catalog:",
		"typescript": "catalog:"
	}
}
